<Window x:Class="TwitchViewersNotifier.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TwitchViewersNotifier"
        mc:Ignorable="d"
        Title="Twitch Оповещения" Height="300" Width="250">
    <Grid Background="#FF772CE8">
        <Button Name="syncButton" Margin="45,118" Click="Sync">
            <TextBlock Text="Синхронизироваться"></TextBlock>
        </Button>
        <Button Name="unsyncButton" Margin="26,118" Visibility="Hidden" Click="UnSync">
            <TextBlock Text="Остановить синхронизацию"/>
        </Button>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="282*">
           
            </ColumnDefinition>
        </Grid.ColumnDefinitions>

    </Grid>
</Window>
